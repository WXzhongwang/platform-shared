/**
 * @platform/ui — 统一组件库封装（在 antd 之上沉淀平台通用组件）。
 * 骨架阶段：重导出 antd，后续逐步沉淀 PageContainer / StatusTag / DeptTree 等业务组件。
 */
export {
  Button,
  Card,
  Layout,
  Menu,
  Table,
  Typography,
  theme,
} from 'antd';
